services:
  postgres:
    image: postgres:17-alpine
    container_name: visualize-postgres
    environment:
      POSTGRES_USER: visualize
      POSTGRES_PASSWORD: visualize
      POSTGRES_DB: visualize
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
